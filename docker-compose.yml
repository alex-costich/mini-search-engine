version: "3.9"
 
services:
  search:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mount corpus so you can re-scrape without rebuilding the image
      - ./corpus.json:/app/corpus.json:ro
    restart: unless-stopped
 